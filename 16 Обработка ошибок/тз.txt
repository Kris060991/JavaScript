Задание 1
Что нужно сделать
Перед вами небольшой кусок кода. Этот код прекрасно исполняется, но в нём имеется скрытая проблема. Отредактируйте код, чтобы проблема перестала быть скрытой. Удалять конструкцию try..catch запрещено.

function hello() {
  console.log('Skill');
}
try { 
  helo();
} catch {}
console.log('complete');

Задание 2
Ниже представлена простая программа, запрашивающая имя пользователя.

Скорректируйте функцию greeting так, чтобы при пустом username сработал alert из catch-блока с сообщением «Имя обязательно для заполнения».

function greeting() {
  const username = prompt("Введите имя пользователя");
}
try { 
  greeting();
} catch(error) { 
  alert(error.message);
}

Задание 3
Что нужно сделать
Перед вами простая функция тестирования типа ошибок. В ней есть проблема. Функция в обоих случаях отображает сообщение в консоли «Обнаружена локальная ошибка». Необходимо исправить функцию testErrorScope так, чтобы во втором случае отображалось сообщение «Обнаружена глобальная ошибка».

function globalError() {
  const error = Error('Глобальная ошибка');
  error.name = 'GlobalError';
  throw error;
}

function localError() {
  const error = Error('Локальная ошибка');
  error.name = 'LocalError';
  throw error;
}

function testErrorScope(fn) {
  try {
    try {
      fn();
    } catch (error) {
      console.log('Обнаружена локальная ошибка');
      console.error(error);
    }
  } catch (error) {
    console.log('Обнаружена глобальная ошибка');
    console.error(error);
  }
}
testErrorScope(localError);
testErrorScope(globalError);

Задание 4
Что нужно сделать
На ваши плечи легла обязанность разработать механизм уведомления пользователя о проблемах с сетью, прежде чем он начнёт обвинять разработчиков в нестабильности приложения.

За основу нужно взять приложение «Видеотека».

Для проверки статуса интернет-соединения следует использовать специальный API-сервис /ping, доступный по адресу https://sb-film.skillbox.cc/ping. Для запроса к API необходимо использовать HTTP-метод POST.

Пример: 

fetch('https://sb-film.skillbox.cc/ping', { method: 'POST' })
Этот API всегда возвращает статус ответа 200 на любой запрос к нему. Приложение должно опрашивать данный API сервера каждые 5 секунд. 

Вам необходимо:

реализовать компонент уведомления пользователя;
при ошибке запроса отображать сообщение «Проблема с сетью»;
если запрос длится более 500 миллисекунд, отображать сообщение о медленном соединении.
Инструменты
Для выполнения задания вам понадобится:

конструкция try..catch;
throw error;
setInterval для организации запросов каждые 5 секунд.
Проблемы с сетью
Если запрос сделать невозможно, необходимо отображать пользователю уведомление до тех пор, пока следующий запрос не будет успешным.

Для тестирования можно воспользоваться панелью инструментов браузера (вкладка Network/«Сеть») и установить значение Offline/«Офлайн». 


Аватар
Назад
16.6 Практическая работа
Цель практической работы
Зафиксировать знания о принципах работы конструкции try..catch и научиться применять эти знания на практике, создав компонент обнаружения проблем с сетью.



Задание 1
Что нужно сделать
Перед вами небольшой кусок кода. Этот код прекрасно исполняется, но в нём имеется скрытая проблема. Отредактируйте код, чтобы проблема перестала быть скрытой. Удалять конструкцию try..catch запрещено.

function hello() {
  console.log('Skill');
}
try { 
  helo();
} catch {}
console.log('complete');
Критерии выполнения задания
В консоли браузера отображается информация о проблеме в коде.



Задание 2
Ниже представлена простая программа, запрашивающая имя пользователя.

Скорректируйте функцию greeting так, чтобы при пустом username сработал alert из catch-блока с сообщением «Имя обязательно для заполнения».

function greeting() {
  const username = prompt("Введите имя пользователя");
}
try { 
  greeting();
} catch(error) { 
  alert(error.message);
}
Критерии выполнения задания
Если в поле не вводить никакого значения, то при подтверждении имени пользователя отобразится alert с сообщением «Имя обязательно для заполнения».
Если в поле ввести имя пользователя и подтвердить, то alert не отобразится.


Задание 3
Что нужно сделать
Перед вами простая функция тестирования типа ошибок. В ней есть проблема. Функция в обоих случаях отображает сообщение в консоли «Обнаружена локальная ошибка». Необходимо исправить функцию testErrorScope так, чтобы во втором случае отображалось сообщение «Обнаружена глобальная ошибка».

function globalError() {
  const error = Error('Глобальная ошибка');
  error.name = 'GlobalError';
  throw error;
}

function localError() {
  const error = Error('Локальная ошибка');
  error.name = 'LocalError';
  throw error;
}

function testErrorScope(fn) {
  try {
    try {
      fn();
    } catch (error) {
      console.log('Обнаружена локальная ошибка');
      console.error(error);
    }
  } catch (error) {
    console.log('Обнаружена глобальная ошибка');
    console.error(error);
  }
}
testErrorScope(localError);
testErrorScope(globalError);
Критерии выполнения задания
Первым сообщением в консоли отображается «Обнаружена локальная ошибка» и сама ошибка, затем сообщение «Обнаружена глобальная ошибка» и сама ошибка.



Задание 4
Что нужно сделать
На ваши плечи легла обязанность разработать механизм уведомления пользователя о проблемах с сетью, прежде чем он начнёт обвинять разработчиков в нестабильности приложения.

За основу нужно взять приложение «Видеотека».

Для проверки статуса интернет-соединения следует использовать специальный API-сервис /ping, доступный по адресу https://sb-film.skillbox.cc/ping. Для запроса к API необходимо использовать HTTP-метод POST.

Пример: 
fetch('https://sb-film.skillbox.cc/ping', { method: 'POST' })
Этот API всегда возвращает статус ответа 200 на любой запрос к нему. Приложение должно опрашивать данный API сервера каждые 5 секунд. 

Вам необходимо:
реализовать компонент уведомления пользователя;
при ошибке запроса отображать сообщение «Проблема с сетью»;
если запрос длится более 500 миллисекунд, отображать сообщение о медленном соединении.
Инструменты
Для выполнения задания вам понадобится:
конструкция try..catch;
throw error;
setInterval для организации запросов каждые 5 секунд.

Проблемы с сетью
Если запрос сделать невозможно, необходимо отображать пользователю уведомление до тех пор, пока следующий запрос не будет успешным.
Для тестирования можно воспользоваться панелью инструментов браузера (вкладка Network/«Сеть») и установить значение Offline/«Офлайн». 

Проверка медленного соединения
Если обнаружено, что время ответа на запрос более 500 миллисекунд, необходимо также отобразить пользователю уведомление, что наблюдается медленное соединение.
Для тестирования можно воспользоваться настройкой сети Slow 3G / «3G (низкая скорость)».

Интернет-соединение в порядке
Когда интернет-соединение в порядке, нужно скрывать сообщение о проблемах. Для проверки убираем выставленные ограничения сети.